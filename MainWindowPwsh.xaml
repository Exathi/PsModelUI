<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    Title="ps7"
    ThemeMode="Dark"
    WindowStartupLocation="CenterScreen"
    Width="640"
    Height="720">
    <Grid>
        <TabControl Margin="5">
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}"
                            Text="&#xF164;" VerticalAlignment="Center"
                            Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                        <TextBlock Text="LongTask" Margin="5"
                            Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                    </StackPanel>
                </TabItem.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="50" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                        <TextBlock Text="{Binding LongTaskView}" FontSize="20" FontWeight="Bold"
                            HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding SampleFunctionView}" FontSize="20"
                            FontWeight="Bold" HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                        <TextBlock Text="{Binding DotSourcedView}" FontSize="20" FontWeight="Bold"
                            HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal"
                        HorizontalAlignment="Center">
                        <Button Name="LongTask" Content="LongTask"
                            Command="{Binding LongTaskCommand}"
                            Style="{DynamicResource AccentButtonStyle}" Margin="5" Width="100"
                            HorizontalAlignment="Center" />
                        <Button Name="Function" Content="Function"
                            Command="{Binding SampleFunctionCommand}"
                            Style="{DynamicResource AccentButtonStyle}" Margin="5" Width="100"
                            HorizontalAlignment="Center" />
                        <Button Name="DotSourced" Content="DotSourced"
                            Command="{Binding DotSourcedCommand}"
                            Style="{DynamicResource AccentButtonStyle}" Margin="5" Width="100"
                            HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}"
                            Text="&#xF16C;" VerticalAlignment="Center"
                            Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                        <TextBlock Text="AnotherTask" Margin="5"
                            Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                    </StackPanel>
                </TabItem.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Grid.Column="0" Name="SnapshotButton"
                        Content="Run long task and log to DataGrid"
                        Command="{Binding AnotherTaskCommand}"
                        Style="{DynamicResource AccentButtonStyle}" Margin="5" Width="250"
                        HorizontalAlignment="Center" />

                    <DataGrid Grid.Row="1" Grid.Column="0" Name="DataGridJobs"
                        ItemsSource="{Binding DataGridJobs}" ColumnWidth="*" IsReadOnly="True"
                        AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id"
                                Width="25"
                                Binding="{Binding Path=Id}" />
                            <DataGridTextColumn Header="Type"
                                Width="50"
                                Binding="{Binding Path=Type}" />
                            <DataGridTextColumn Header="Time"
                                Width="*"
                                Binding="{Binding Path=Time}" />
                            <DataGridTextColumn Header="Snapshot"
                                Width="*"
                                Binding="{Binding Path=Snapshot}" />
                            <DataGridTextColumn Header="Method"
                                Width="*"
                                Binding="{Binding Path=Method}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}"
                            Text="&#xEB52;" VerticalAlignment="Center"
                            Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                        <TextBlock Text="ProgressBar" Margin="5"
                            Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                    </StackPanel>
                </TabItem.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="30" />
                        <RowDefinition Height="30" />
                        <RowDefinition Height="50" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="1" Text="{Binding Path=Status}" Margin="5"
                        Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                    <ProgressBar Grid.Row="2" Value="{Binding StatusPercent}" Minimum="0"
                        Maximum="100" Height="20" />
                    <StackPanel Grid.Row="3" Orientation="Horizontal">
                        <Button Name="ProgressButtonStart" Content="Start"
                            Command="{Binding ProgressBarCommand}"
                            Style="{DynamicResource AccentButtonStyle}" Margin="5" Width="80"
                            HorizontalAlignment="Left" />
                        <Button Name="ProgressButtonPause" Content="{Binding Status}"
                            Command="{Binding ProgressPauseCommand}" Margin="5" Width="80"
                            HorizontalAlignment="Left" />
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
